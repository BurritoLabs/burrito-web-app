import PageShell from "./PageShell"
import SwapPanel from "./components/SwapPanel"

const Swap = () => {
  return (
    <PageShell title="Swap">
      <SwapPanel />
    </PageShell>
  )
}

export default Swap
